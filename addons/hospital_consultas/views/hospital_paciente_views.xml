<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_hospital_paciente_list" model="ir.ui.view">
        <field name="name">hospital.paciente.list</field>
        <field name="model">hospital.paciente</field>
        <field name="arch" type="xml">
            <list>
                <field name="nombre"/>
                <field name="apellidos"/>
                <field name="sintomas"/>
            </list>
        </field>
    </record>

    <record id="view_hospital_paciente_form" model="ir.ui.view">
        <field name="name">hospital.paciente.form</field>
        <field name="model">hospital.paciente</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="nombre"/>
                        <field name="apellidos"/>
                        <field name="sintomas"/>
                    </group>
                    <group string="Consultas">
                        <field name="consulta_ids" readonly="1">
                            <list>
                                <field name="fecha_consulta"/>
                                <field name="medico_id"/>
                                <field name="diagnostico"/>
                            </list>
                        </field>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_hospital_paciente_search" model="ir.ui.view">
        <field name="name">hospital.paciente.search</field>
        <field name="model">hospital.paciente</field>
        <field name="arch" type="xml">
            <search>
                <field name="nombre"/>
                <field name="apellidos"/>
            </search>
        </field>
    </record>

    <record id="action_hospital_paciente" model="ir.actions.act_window">
        <field name="name">Pacientes</field>
        <field name="res_model">hospital.paciente</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_hospital_paciente_search"/>
    </record>
</odoo>
